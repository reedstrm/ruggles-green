Repository global todo items


* Add API method to lookup resources by size/hash

* Make the IdUtil test to work (requires GAE test jars)
* Revisit all the TODO's and address the ones that need to be addressed for the Pilot (if any)

* Clean up all compilations and startup warnings
* Delete the temp servlets as soon as the actual API will be ready.

Post Pilot
* (Security) Add verifications that uploaded completion method are actually called from blobstore and not directly by a user.
* Incoming XML update/validation (modify incoming XML with repository values (e.g. version)).
* parameter validations (in the servlets and the service)
* User authentication (in servlets)
* User authorization (in service)
* Impose size limits on posted items
* Perfornamce optimization
* Support for search
* Add ability to take down (e.g. 'invisible' flag in entities)

Done
+ Add support for collections
+ Allways return abosolure resource upload URL
+ Have all schema entities using Key's rathar than longs
+ Refactor common code (e.g. module version key construction).
+ Proper logging
+ Basic support for exports
+ Replace @Nullable with java standard @Nullable
+ Verify the safety and consistency of the handling of transactions that left opened.
+ When looking up an entity, distinguish between 'not found' JDOObjectNotFoundException JDO exception and all others.
+ When overwriting an existing export, make sure to delete the old blob
+ Validate uploaded export mime type against the expected mime type of this export type.
+ Use same parameter mecanism in the upload completion servlets of resources and exports.
+ Migrate the entities to non JDO API an use ancestor queries for exports (transactional).
+ Move all the repository configuration params (exports, max sizes, etc) to a single config class.
+ Add to repository service ability to write multiple entities at once and use where applicable.
+ Add module/collection method version param for version conflict detection
+ Add service methods to enumerate all collections
+ Add service methods to enumerate all modules
+ (ETA Aug 15 2011) Remove repository service relative blobstore URL prefixing when app engine 1.3.5 will be available.
+ Return creationTime from all entities (in getInfo).
+ Add service methods to enumerate all resources
+ Add API capability for the migrator to preserve old module and collectins ids.
+ return size and md5 hash for resource and export info results


